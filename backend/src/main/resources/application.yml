spring:
  datasource:
    url: jdbc:mysql://localhost:3306/paymedia_crm
    username: root
    password: Arafath0313

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: paymedia-crm

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:placeholder@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:placeholder}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: PayMediaCRMSecretKeyForJwtAuthentication2026PayMedia
  expiration: 86400000